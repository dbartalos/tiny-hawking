sudo: required
services:
  - docker

before_install:
  - docker build -t dbartalos/react-test -f ./client/Dockerfile.dev ./client/Dockerfile.dev

script:
  - docker run dbartalos/react-test npm test -- --coverage

after_success:
  - docker build -t dbartalos/multi-client ./client
  - docker build -t dbartalos/multi-nginx ./nginx
  - docker build -t dbartalos/multi-server ./server
  - docker build -t dbartalos/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push dbartalos/multi-client
  - docker push dbartalos/multi-nginx
  - docker push dbartalos/multi-server
  - docker push dbartalos/multi-worker
